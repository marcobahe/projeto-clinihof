# ===========================================
# CliniHOF - Variáveis de Ambiente
# ===========================================
# Copie este arquivo para .env e preencha os valores
# cp .env.example .env
# ===========================================

# ---- Banco de Dados (PostgreSQL) ----
# URL de conexão com o banco PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://usuario:senha@localhost:5432/clinihof"

# ---- Autenticação (NextAuth.js) ----
# Chave secreta para criptografia de sessões
# Gere uma chave segura com: openssl rand -base64 32
NEXTAUTH_SECRET="sua-chave-secreta-aqui"

# URL base da aplicação (ajuste para produção)
NEXTAUTH_URL="http://localhost:3000"

# ---- AWS S3 (Upload de arquivos) ----
# Configurações para upload de avatares e arquivos
AWS_PROFILE="default"
AWS_REGION="us-east-1"
AWS_BUCKET_NAME="seu-bucket-s3"
AWS_FOLDER_PREFIX="clinihof/"

# ---- Google OAuth & Calendar Sync ----
# Para login com Google e sincronização com Google Calendar
# Configure no Google Cloud Console:
# 1. Enable Google Calendar API
# 2. Create OAuth 2.0 credentials
# 3. Add redirect URI: https://yourdomain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
